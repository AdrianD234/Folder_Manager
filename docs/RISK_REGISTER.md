# Risk Register

## Risk Levels
- High: Could violate safety, privacy, or user trust.
- Medium: Could cause incorrect behavior or poor usability.
- Low: Manageable implementation or maintenance risk.

## Risks
| ID | Risk | Level | Mitigation | Status |
| --- | --- | --- | --- | --- |
| R001 | Prompt fatigue from too many file events | High | Deterministic triage before prompting, batch suppression, selected folders only, confidence thresholds, one-popup-at-a-time display | Partially mitigated: Milestones 3, 4, 7, 13, and 14 add triage, batch threshold suppression, explicit-folder watcher scope, candidate queue gating, settings validation, candidate/audit display, and one-at-a-time popup routing |
| R002 | Incorrectly processing archive extractions | High | Batch detection, extraction pattern rules, no individual prompts above thresholds | Partially mitigated: Milestone 4 classifies archive extraction bursts and suppresses individual prompts above threshold |
| R003 | Incorrectly processing OneDrive sync bursts | High | Burst detection, sync path heuristics, batch audit instead of prompts | Partially mitigated: Milestone 4 classifies OneDrive-like bursts and suppresses individual prompts above threshold |
| R004 | Incorrectly processing compiler/build noise | High | Ignore directories and extensions, development folder tests | Partially mitigated: Milestone 3 suppresses build folders and generated extensions |
| R005 | Incorrectly processing Codex-generated repo files | High | Repo/build ignore rules, folder-level context, no child-file tagging by default | Partially mitigated: Milestone 3 suppresses repo/build/package folders and returns folder-level context recommendations |
| R006 | File locks and partial downloads | High | Stability engine, debounce, lock checks, partial extension rules | Partially mitigated: Milestone 4 adds stability, debounce, local lock snapshot, and partial/temp extension decisions; Milestone 7 prevents unstable or partial events from entering the candidate queue |
| R007 | Accidental overwrite | High | Central safe operation service, destination validation, conflict resolver, stale destination recheck, confirmation UI, tests | Partially mitigated: Milestones 5 and 15 refuse overwrites, generate non-overwriting conflict candidates, show WPF preview/confirmation state, and test confirmation refusal plus confirmed temp-file move |
| R008 | Incorrect undo | High | Identity verification, original-path conflict checks, action/undo records, undo UI tests | Partially mitigated: Milestones 5 and 15 verify file identity and original-path availability in temp-directory tests and add a WPF undo list/action workflow with success, conflict, and identity-mismatch tests |
| R009 | Private metadata leakage | High | SQLite source of truth, no embedded metadata, sidecar avoidance, secret redaction, local logs only, tests asserting selected files are unchanged | Partially mitigated: Milestone 6 manual metadata capture writes only SQLite rows and action audit records in automated temp-file tests; Milestone 9 redacts OpenAI API keys from persistable provider error fields; Milestone 14 proves candidate popup metadata and transcript saves do not modify temp files or create sidecars; Milestone 16 structured logging tests prove raw transcripts, provider metadata, audio paths, private detail payloads, and lifecycle secret-like fields are not mirrored to local JSON-lines logs |
| R010 | API cost surprise | Medium | Providers disabled by default, visible settings, no background API calls without opt-in | Partially mitigated: Milestone 8 adds manual text, fake provider tests, and local provider not-configured behavior without live API calls; Milestone 9 keeps OpenAI disabled by default and tests success/failure with fake HTTP only |
| R011 | Search result ambiguity | Medium | Rank results, show ambiguous results, confirm bulk open, and constrain confirmed launch actions to existing local filesystem paths | Partially mitigated: Milestones 10, 11, and 15 add deterministic SQLite ranking, optional Everything enrichment, structured result display, explicit selected-file/folder open actions, fake-launcher tests, and confirmation before bulk open; the pre-smoke hardening pass rejects URL, shell, relative, nonexistent, and wrong-target launch paths before OS launch |
| R012 | Hotkey conflicts | Medium | Configurable hotkeys, visible failure when registration fails, nonfatal registration failure | Partially mitigated: Milestone 12 adds a nonfatal Win32 hotkey boundary; user-facing hotkey configuration and manual smoke testing remain |
| R013 | Database corruption | High | Explicit migrations, `schema_migrations`, temp SQLite tests, future backups/exports, clear recovery guidance | Partially mitigated: initial schema and migration tests added |
| R014 | Large file hashing performance | Medium | Hash ordinary stable files, defer large hashes, configurable thresholds | Partially mitigated: Milestone 4 defers hashing above the initial 100 MB threshold |
| R015 | User trust erosion from unsafe automation | High | Explicit confirmation, logs, undo, conservative defaults, manual mode | Partially mitigated: Milestones 5 and 15 enforce confirmation objects, WPF confirmation surfaces, action/undo audit rows, undo UI checks, and confirmed retrieval actions; watcher code still performs no file mutation and rejects broad watcher roots |
| R016 | SDK/toolchain mismatch | Medium | Pin SDK in `global.json`, verify `dotnet --info`, document blocker in status | Mitigated: .NET SDK 8.0.421 verified locally |
| R017 | External dependency drift | Medium | Central package versions, lock files, decision notes for dependencies, commit lock-file updates after restore | Open |
| R018 | NuGet restore unavailable in restricted environments | Medium | Use pinned central versions, commit lock files after successful restore, prefer `tools/validate.ps1` to redirect .NET and NuGet home/cache paths into ignored repo-local folders, disable the online NuGet audit lookup only for sandbox validation, and record any remaining network blocker exactly | Mitigation improved and verified: latest repo-local validation passed with SDK 8.0.421, restore up to date, 0 build warnings/errors, and 156 passing tests; script validation result is tracked in `docs/STATUS.md` |
| R019 | File watcher event duplication creates duplicate candidates | Medium | Keep watcher event source separate from Core candidate queue, compose with stability and batch decisions, audit processed outcomes, and add future persistence-backed dedupe before popup display | Partially mitigated: Milestone 13 adds event audit rows and UI-facing queue refresh, but not persistent dedupe |
| R020 | Misconfiguration watches a drive root or whole user profile | High | Watch only enabled `intake_folders`, reject drive roots and whole-user-profile roots in settings and watcher construction, keep Downloads as disabled suggestion until user accepts | Partially mitigated: Milestones 7 and 13 add rejection tests for drive roots, disabled Downloads suggestion, SQLite-backed folder settings, broad-root/repository-marker validation, and explicit enabled-folder watcher construction |
| R021 | Audio temp cleanup deletes the wrong file | High | Confine temp-audio cleanup to configured temp-audio root, use opaque generated names, refuse cleanup outside root, and test refusal with temp directories only | Partially mitigated: Milestone 8 adds `AudioTempFileService` root checks and tests for default deletion, explicit retention, failure retention, and unsafe-path refusal; Milestone 9 verifies OpenAI-provider success can use the same default cleanup policy |
| R022 | Microphone/audio integration complexity | Medium | Keep provider boundary and manual text fallback first; defer hardware recording until UI/OS integration can be tested safely | Open: Milestone 8 does not implement real microphone capture |
| R023 | Everything CLI path discovery fragility | Medium | Keep Everything disabled by default, support configured path and PATH discovery behind a resolver, require allowed roots before invoking `es.exe`, fake process tests, and fail closed to SQLite-only search when unavailable or unscoped | Partially mitigated: Milestone 11 adds resolver, missing-executable tests, unscoped-provider tests, and allowed-root filtering; no UI configuration yet |
| R024 | UI claims exceed proven behavior | High | Clearly mark manual smoke tests not run, keep known limitations current, avoid claiming untested OS integration as passed, and require an explicit approval boundary before interactive smoke is run | Partially mitigated: Milestones 12 through 16 update manual smoke status, add automated App view-model tests, and document the interactive smoke approval boundary; interactive tray/settings/watcher/popup/filing/retrieval smoke tests remain deferred pending user approval |
| R025 | Foundation milestones complete before product workflows are integrated | High | Maintain `docs/COMPLETION_AUDIT.md`, extend `docs/PLAN.md` with post-12 remediation milestones, and keep `docs/STATUS.md` focused on remaining integrated workflows rather than claiming goal completion | Partially mitigated: Milestones 13, 14, and 15 complete the integration remediation milestones by automated validation; Milestone 16 no-RID publish, runtime-specific publish, artifact checks, and final docs are in place, but manual smoke remains required or user-deferred before the long-running goal can be considered complete |
| R026 | Watcher candidate queue loses or duplicates user-facing prompts across app restarts | Medium | Add persistent event audit and UI-facing queue handling before relying on watcher candidates in the tray app; add in-memory duplicate suppression before popup display and keep durable candidate persistence as a follow-up | Partially mitigated: Milestone 13 persists processed event audit rows and exposes current in-memory candidates; Milestone 14 routes current in-memory candidates into one popup at a time; the pre-smoke hardening pass adds normalized-path in-memory deduplication inside the observed/stable window. Restart still loses pending candidate UI state |
| R027 | Popup dismissal loses user intent without audit | Medium | Treat close/skip as an explicit skipped action, record action details in SQLite, and avoid creating metadata on skip | Partially mitigated: Milestone 14 writes `IntakeCandidateSkipped` action rows for skip/dismiss and tests that no metadata or file record is created by skip |
| R028 | Runtime-specific publish blocked by restricted network/runtime-pack cache | Medium | Keep validation repo-local, document exact network/cache failures, provide a no-RID framework-dependent publish fallback, keep `artifacts/` ignored, and rerun runtime-specific publish when NuGet access or cached runtime packs are available | Mitigated for current Milestone 16 state: the unapproved sandbox run failed with `NU1801`/`NU1101`, then the approved NuGet-access `tools/publish-local.ps1 -RuntimeSpecific` run restored runtime packs and passed |
| R029 | Release artifact accidentally includes private app data, logs, audio, secrets, or repo-local cache files | High | Keep local app data and `artifacts/` ignored, do not package local runtime state, and run `tools/check-publish-artifact.ps1` after publish | Mitigated by Milestone 16 artifact safety checker for current no-RID and runtime-specific publish outputs; repeat for any installer or alternate artifact |
| R030 | Manual smoke fixture setup mutates the wrong folder | Medium | Keep fixture generation under system temp, refuse non-temp roots, refuse existing roots, never delete or overwrite files, and support `-PlanOnly` preview | Mitigated for `tools/new-smoke-fixtures.ps1`; actual interactive smoke execution still requires explicit user approval |
| R031 | Release readiness documentation drifts from actual blocker state | Medium | Keep `docs/STATUS.md`, `docs/COMPLETION_AUDIT.md`, `docs/MANUAL_SMOKE_TESTS.md`, `docs/PACKAGING.md`, and risk entries aligned; run `tools/check-release-readiness.ps1` after Milestone 16 readiness edits | Mitigated for current Milestone 16 state by a read-only release-readiness checker |
| R032 | Structured logs mirror raw private context | High | Log paths and workflow identifiers for auditability, but log only lengths/booleans for notes, transcripts, provider metadata, raw voice/search text, audio paths, source URLs, and action details; lifecycle logs must use command names, component names, counts, booleans, and exception types only; redact secret-looking and private payload-shaped keys | Mitigated for current structured logging wrapper and App shell lifecycle logger by temp-path tests that verify raw transcript text, provider metadata, audio file names, source URLs, raw search/voice text, action detail payloads, API-key-shaped fields, and OpenAI-style secrets are not mirrored |
| R033 | Manual smoke evidence is incomplete or overwritten | Medium | Generate smoke run reports under ignored `artifacts/`, refuse output paths outside `artifacts/`, refuse overwrites, and start every test as `Not run` until a human records actual results | Mitigated for report setup by `tools/new-smoke-run-report.ps1`; actual interactive smoke evidence still requires a user-approved or user-run pass |
| R034 | Confirmed retrieval launches an unsafe shell target | High | Keep confirmation requirements and validate launch targets immediately before OS launch; reject URLs, `shell:` targets, `file:` URI strings, relative paths, nonexistent files/folders, and wrong target types; test with fake process launcher only | Mitigated by pre-smoke hardening tests for `WindowsFileLaunchService`; interactive OS launch behavior still requires user-approved manual smoke |
| R035 | App-performed move/rename events re-enter watcher intake | High | Register app moves, renames, and undo moves in a central own-operation suppression registry and pass active suppressions into watcher intake processing | Mitigated for current in-session operations by `OwnOperationSuppressionRegistry`, `SafeFileOperationExecutor`, and `IntakeWatcherCoordinator` wiring; restart persistence is not required because suppression windows are short |
| R036 | CI gives a false sense of release readiness | Medium | Keep CI limited to restore/build/test and document that publish checks and interactive smoke remain separate local/user-approved release gates | Mitigated by minimal Windows CI that does not publish artifacts, launch the app, call providers, or touch user folders |

## Review Cadence
Review this register after every milestone. Add new risks immediately when discovered.
