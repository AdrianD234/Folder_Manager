<Window x:Class="FileIntakeAssistant.App.IntakePopupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="File Intake"
        Height="640"
        Width="760"
        MinHeight="560"
        MinWidth="680"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{Binding FileName}"
                   FontSize="19"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap" />

        <Grid Grid.Row="1" Margin="0,12,0,14">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="92" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Path" Margin="0,0,12,4" />
            <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Path}" TextWrapping="Wrap" Margin="0,0,0,4" />

            <TextBlock Grid.Row="1" Grid.Column="0" Text="Type" Margin="0,0,12,4" />
            <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Extension}" Margin="0,0,0,4" />

            <TextBlock Grid.Row="2" Grid.Column="0" Text="Size" Margin="0,0,12,4" />
            <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding SizeBytes}" Margin="0,0,0,4" />

            <TextBlock Grid.Row="3" Grid.Column="0" Text="Triage" Margin="0,0,12,4" />
            <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding TriageReason}" TextWrapping="Wrap" Margin="0,0,0,4" />

            <TextBlock Grid.Row="4" Grid.Column="0" Text="Provider" Margin="0,0,12,0" />
            <TextBlock Grid.Row="4" Grid.Column="1" Text="{Binding ProviderStatusText}" TextWrapping="Wrap" />
        </Grid>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="92" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Text="Note"
                       Margin="0,4,12,0" />
            <TextBox Grid.Row="0"
                     Grid.Column="1"
                     Text="{Binding UserNote, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MinHeight="92"
                     VerticalScrollBarVisibility="Auto" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Text="Transcript"
                       Margin="0,14,12,0" />
            <TextBox Grid.Row="1"
                     Grid.Column="1"
                     Text="{Binding TranscriptText, UpdateSourceTrigger=PropertyChanged}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     MinHeight="76"
                     Margin="0,10,0,0"
                     VerticalScrollBarVisibility="Auto" />

            <TextBlock Grid.Row="2"
                       Grid.Column="0"
                       Text="Relevance"
                       VerticalAlignment="Center"
                       Margin="0,14,12,0" />
            <ComboBox Grid.Row="2"
                      Grid.Column="1"
                      SelectedValue="{Binding Relevance, UpdateSourceTrigger=PropertyChanged}"
                      SelectedValuePath="Content"
                      Margin="0,10,0,0"
                      MinHeight="30">
                <ComboBoxItem Content="low" />
                <ComboBoxItem Content="medium" IsSelected="True" />
                <ComboBoxItem Content="high" />
            </ComboBox>

            <TextBlock Grid.Row="3"
                       Grid.Column="0"
                       Text="Project"
                       VerticalAlignment="Center"
                       Margin="0,14,12,0" />
            <TextBox Grid.Row="3"
                     Grid.Column="1"
                     Text="{Binding Project, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,10,0,0"
                     MinHeight="30"
                     VerticalContentAlignment="Center" />

            <TextBlock Grid.Row="4"
                       Grid.Column="0"
                       Text="Topic"
                       VerticalAlignment="Center"
                       Margin="0,14,12,0" />
            <TextBox Grid.Row="4"
                     Grid.Column="1"
                     Text="{Binding Topic, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,10,0,0"
                     MinHeight="30"
                     VerticalContentAlignment="Center" />

            <Grid Grid.Row="5" Grid.ColumnSpan="2" Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="92" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="88" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="Tags"
                           VerticalAlignment="Center"
                           Margin="0,0,12,0" />
                <TextBox Grid.Column="1"
                         Text="{Binding Tags, UpdateSourceTrigger=PropertyChanged}"
                         MinHeight="30"
                         VerticalContentAlignment="Center" />
                <TextBlock Grid.Column="2"
                           Text="Source URL"
                           VerticalAlignment="Center"
                           Margin="16,0,12,0" />
                <TextBox Grid.Column="3"
                         Text="{Binding SourceUrl, UpdateSourceTrigger=PropertyChanged}"
                         MinHeight="30"
                         VerticalContentAlignment="Center" />
            </Grid>
        </Grid>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="{Binding StatusMessage}"
                       VerticalAlignment="Center" />
            <Button Grid.Column="1"
                    Content="Skip"
                    Command="{Binding SkipCommand}"
                    MinWidth="94"
                    MinHeight="34"
                    Margin="0,0,10,0" />
            <Button Grid.Column="2"
                    Content="Save Metadata"
                    Command="{Binding SaveCommand}"
                    MinWidth="128"
                    MinHeight="34" />
        </Grid>
    </Grid>
</Window>
